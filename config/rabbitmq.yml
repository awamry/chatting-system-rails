development:
  host: localhost
  port: 5672
  db: 0
production:
  host: <%= ENV.fetch("RABBITMQ_HOST") { 'rabbitmq' } %>
  port: <%= ENV.fetch("REDIS_PORT") { 5672 } %>
test:
  host: fake
  port: rabbitmq
